const assert = require('assert');

const plugin = require('../index');

describe('svrx-plugin-test-plugin', () => {
  it('should be a object', () => {
    assert.strictEqual(typeof plugin, 'object');
  });
  // TODO: add more tests
});
